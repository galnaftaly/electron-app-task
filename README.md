# Electron and React Task

## Setup and build
```bash
git clone https://github.com/galnaftaly/electron-task-app.git
cd electron-task-app
npm install
npm run package
```

Then the application should be find inside directory: <root-directory>/release/win-unpacked